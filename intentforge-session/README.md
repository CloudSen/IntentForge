# intentforge-session

Session management aggregation module for conversation lifecycle, message history, and pluggable session manager runtime.
Includes `intentforge-session-core` for contracts and models, plus `intentforge-session-local` for local runtime implementations.
